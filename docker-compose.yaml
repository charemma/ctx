services:
  postgres:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ctx
      POSTGRES_USER: ctx
      POSTGRES_PASSWORD: ctx
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
